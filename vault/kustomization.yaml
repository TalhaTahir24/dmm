resources:
  - ingress.yaml

helmCharts:
  - name: vault
    repo: https://helm.releases.hashicorp.com
    version: 0.27.0
    releaseName: vault
    namespace: web
    valuesFile: values.yaml
